/**
 * 'kinetik/react' — React adapter entry point.
 *
 *   import { DndContext, useSortable, useSortableContainer } from 'kinetik/react'
 *
 * Includes everything from 'kinetik/core' plus the React hooks.
 */
export * from '../index.js'